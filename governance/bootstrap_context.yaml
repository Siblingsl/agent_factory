repo_mode: remote
bootstrap_stage: ci_gate_bootstrap_pending_validation
project_status: bootstrap_blocked
next_action: finish_ci_gate_bootstrap
notes: >
  Remote repository mode is enabled. Branch protection must remain enforced on main.
remote_enforcement:
  pr_doc_drift_gate: enabled_in_ci
  branch_protection_guard: enabled_in_ci
